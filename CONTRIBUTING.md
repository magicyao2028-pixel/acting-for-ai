# 参与贡献

欢迎补充具体情绪、关系互动、动作质感、来源纠错和接入经验。贡献应能服务实际表演表达。

1. 在 `cards/` 增加或修改 JSON 条目。沿用现有字段，已有编号永久保留；新编号使用对应分类当前最大编号加一。
2. 写清处境、可见动作、适用边界以及来源。原创推导应标明原创；外部资料用短摘要和链接，不搬运课程全文或无授权视频。
3. 反馈按真实情况标注：作者／贡献者实际使用、文字示例、生成视频记录或尚待尝试。新贡献不能借用维护者的实践状态冒充自己已验证；未实测的新条目使用 `editorial_reference`，保留其真实范围。
4. 运行 `python scripts/library.py check`、`python scripts/library.py build` 和 `python scripts/library.py build --check`。
5. 提交时说明解决的问题、涉及编号、来源变化和验证范围。

`data/` 与 `docs/cards/` 是自动导出文件，不直接编辑。不要修改别人的原始实践记录；发现问题时补充更正、版本和原因。

贡献的原创文档与条目按仓库 CC BY 4.0 发布，代码按 MIT 发布。请仅提交你有权贡献的材料。第三方来源保留原作者归属。
