# 七天维护规则

本仓库由维护者已有的七天任务维护。GitHub CI 只做一致性检查；本文件不创建额外计划，也不调用视频生成。

## 每轮执行

1. 读取本文件、版本、索引、最近更新记录和必要条目，检查工作区与远程状态。
2. 复查中英文公开资料，优先处理重复、过时来源、缺少的动作表达和真实使用反馈。每轮新增最多 9 条，修正既有内容不计入新增上限；没有合格新增不凑数。
3. 修改 `cards/*.json` 和需要更新的来源记录。给新条目写真实来源及验证范围，不继承整个资料库的作者验证作为单条实测证明。
4. 运行结构检查、重新导出、导出一致性检查及必要的离线测试。检查公开文件中无本机路径、私密项目内容或凭据。
5. 有实质变化时更新版本、`CHANGELOG.md`、`CITATION.cff`，写清新增／修改编号、资料来源与验证范围。版本引用与条目更新日期保持一致。无变化时不制造空提交和空版本。
6. 仅在检查通过后提交本轮明确涉及的文件，按已授权范围同步 GitHub；记录本地提交和远程提交是否一致。不要强推，不要覆盖远程独立修改，不要把本地提交冒充已发布。

## 与作者内部资料库的边界

公开条目有独立版本号。作者内部 Skill 的确认、备份与同步规则继续有效；本任务获得的公开仓库更新授权不等于任意修改内部 Skill 的授权。

内部资料如已有新确认版本，检查差异并择取可公开内容，不重新全量导入、重编号或覆盖外部贡献。来源快照保留在 `sources/origin-manifest.json`；公开条目可继续独立演进。

## 发布失败

认证失败或远程仓库尚未创建：保留已经检查通过的本地成果，记录“本地完成，远程待同步”，不要反复登录、索要明文 Token 或另建仓库。

远程变化或工作区有不明修改：保留全部工作，在独立分支整理自己的改动；无法安全合并时报告具体冲突，不能重置、强推或丢弃文件。

## 通知

有实质更新时说明新增／修正内容和远程同步结果；无变化且无待处理故障时保持安静。失败、需要用户操作或长期未同步时明确说明原因。

暂不制作完整视频案例，不自动调用付费或本地生成算力。已有作者实践验证保留；未公开视频附件不应被改写成“未经验证”。
